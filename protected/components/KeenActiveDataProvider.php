<?php

class KeenActiveDataProvider extends CActiveDataProvider {
	/**
	 * Preload models into $data.
	 *
	 * @param mixed $relations Specifies the relations to be preloaded. If a string,
	 * the name of a relation to preload. If an array of strings, a list of relations as
	 * specified for {@link CActiveRecord:relations()}.
	 */
	public function preload($relations) {

	}
}